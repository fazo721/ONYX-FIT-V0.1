// ONYX FIT v0.1 — global progression dashboard
const STORAGE='onyx_v01';
const $=s=>document.querySelector(s);
const read=()=>{try{return JSON.parse(localStorage.getItem(STORAGE)||'{}')||{}}catch{return{}}};
const fmt=n=>Math.round(Number(n)||0).toLocaleString('fr-FR');
const d=v=>{const x=new Date(v);return Number.isNaN(x.getTime())?null:x};
function injectStyle(){if($('#progressDashboardStyle'))return;const s=document.createElement('style');s.id='progressDashboardStyle';s.textContent=`
.progress-overview{margin-bottom:12px}.progress-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin-top:10px}.progress-kpi{background:#0a0a0a;border:1px solid #262626;border-radius:14px;padding:10px}.progress-kpi span{display:block;color:#777;font-size:.62rem}.progress-kpi b{display:block;margin-top:4px;font-size:1rem}.coach-insight{margin-top:10px;background:linear-gradient(145deg,#17110d,#0b0b0b);border:1px solid #4c2a17;border-radius:14px;padding:11px}.coach-insight b{display:block}.coach-insight p{margin:5px 0 0;color:#aaa;font-size:.78rem;line-height:1.4}.progress-list{display:grid;gap:7px;margin-top:10px}.progress-row{display:grid;grid-template-columns:1fr auto;gap:8px;align-items:center;background:#0a0a0a;border:1px solid #252525;border-radius:12px;padding:9px}.progress-row span{display:block;color:#777;font-size:.68rem}.progress-row b{font-size:.82rem}.progress-row strong{font-size:.82rem}.progress-positive{color:#73dc9a}.progress-neutral{color:#ff9b62}@media(max-width:430px){.progress-grid{grid-template-columns:repeat(2,1fr)}}
`;document.head.appendChild(s)}
function history(){const h=read().history;return Array.isArray(h)?h.filter(x=>x&&Array.isArray(x.sets)):[]}
function recent(days){const cut=Date.now()-days*86400000;return history().filter(h=>{const x=d(h.date);return x&&x.getTime()>=cut})}
function sessionVolume(h){return (h.sets||[]).reduce((a,s)=>a+(+s.weight||0)*(+s.reps||0),0)}
function estimated1RM(w,r){w=+w||0;r=+r||0;if(!w||!r)return 0;return w*(1+r/30)}
function exerciseProgress(){const map=new Map();for(const h of history()){for(const s of h.sets||[]){if(!s?.exercise)continue;const key=s.exercise,rm=estimated1RM(s.weight,s.reps);if(!map.has(key))map.set(key,[]);map.get(key).push({date:h.date,rm,weight:+s.weight||0,reps:+s.reps||0})}}const out=[];for(const [name,sets] of map){sets.sort((a,b)=>new Date(a.date)-new Date(b.date));if(sets.length<2)continue;const first=sets[0],last=sets.at(-1),best=sets.reduce((a,b)=>b.rm>a.rm?b:a,sets[0]);const pct=first.rm>0?(last.rm-first.rm)/first.rm*100:0;out.push({name,pct,last,best})}return out.sort((a,b)=>b.pct-a.pct)}
function insight(){const h7=recent(7),h30=recent(30),p=read().profile||{},target=+p.sessions||0;const completed=h7.filter(x=>x.completed!==false).length,partial=h7.filter(x=>x.interrupted).length;if(target&&completed<target)return `Tu as ${completed}/${target} séances terminées cette semaine. Priorité : régularité avant d’ajouter du volume.`;if(partial>1)return `Plusieurs séances ont été interrompues cette semaine. Garde le programme, mais raccourcis si besoin au lieu de sauter complètement.`;const prog=exerciseProgress().filter(x=>x.pct>2);if(prog.length)return `${prog[0].name} progresse le plus en ce moment (+${prog[0].pct.toFixed(1)} % d’e1RM). Continue sur cette dynamique.`;if(h30.length>=3)return `La base est régulière. Cherche maintenant de petites progressions propres : +1 rep ou +1 à 2 kg quand le RPE le permet.`;return `Encore peu de données. Fais quelques séances complètes pour qu’ONYX puisse détecter une vraie tendance.`}
function html(){const h=history(),h7=recent(7),h30=recent(30);if(!h.length)return'';const volume30=h30.reduce((a,x)=>a+sessionVolume(x),0),duration30=h30.reduce((a,x)=>a+(+x.duration||0),0),completed30=h30.filter(x=>x.completed!==false).length,partial30=h30.filter(x=>x.interrupted).length;const top=exerciseProgress().slice(0,5);return `<div class="card progress-overview"><div class="eyebrow">VUE D’ENSEMBLE</div><h2>Progression</h2><div class="progress-grid"><div class="progress-kpi"><span>SÉANCES 7 J</span><b>${h7.length}</b></div><div class="progress-kpi"><span>SÉANCES 30 J</span><b>${completed30}</b></div><div class="progress-kpi"><span>VOLUME 30 J</span><b>${fmt(volume30)} kg</b></div><div class="progress-kpi"><span>TEMPS 30 J</span><b>${Math.round(duration30/60)} min</b></div></div><div class="coach-insight"><b>⚡ Analyse ONYX</b><p>${insight()}</p></div>${top.length?`<div class="progress-list">${top.map(x=>`<div class="progress-row"><div><b>${x.name}</b><span>e1RM estimé actuel ${x.last.rm.toFixed(1)} kg · meilleur ${x.best.rm.toFixed(1)} kg</span></div><strong class="${x.pct>0?'progress-positive':'progress-neutral'}">${x.pct>=0?'+':''}${x.pct.toFixed(1)} %</strong></div>`).join('')}</div>`:''}${partial30?`<p class="muted" style="margin-top:9px">${partial30} séance${partial30>1?'s':''} interrompue${partial30>1?'s':''} conservée${partial30>1?'s':''} dans les statistiques.</p>`:''}</div>`}
function mount(){injectStyle();const root=$('#trainingStatsBox');if(!root||root.hidden)return;const existing=$('#globalProgressOverview');if(existing)existing.remove();const wrap=document.createElement('div');wrap.id='globalProgressOverview';wrap.innerHTML=html();if(wrap.firstElementChild)root.prepend(wrap)}
const obs=new MutationObserver(()=>{if($('#trainingStatsBox')&&!$('#trainingStatsBox').hidden)setTimeout(mount,30)});obs.observe(document.documentElement,{childList:true,subtree:true,attributes:true,attributeFilter:['hidden']});window.addEventListener('DOMContentLoaded',()=>setTimeout(mount,300));
